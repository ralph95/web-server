version: "3.8"

services:
  node-server:
    build: .
    container_name: node-server-container
    ports:
      - "5000:5000"
    restart: always
    networks:
      - app_network
    environment:
      - POCKETBASE_URL=http://192.168.1.150:8090

networks:
  app_network:
    external: true
