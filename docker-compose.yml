version: "3.8"

services:
  node-server:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: node-server-container
    ports:
      - "5000:5000" # Map host:container
    restart: always
    networks:
      - app_network

networks:
  app_network:
    driver: bridge
